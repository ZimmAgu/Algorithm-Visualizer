function dijkstra (graph, startNode, endNode) {
    console.log(endNode)
    // // Step 1: 

    // let distances = {};                 // // tracks distances from the start node using an object
    // distances[endNode] = Infinity;      // The distances of all of the non start nodes are initially set to infinity
    
}

export default dijkstra;

